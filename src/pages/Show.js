import TransactionDetails from "../components/Show/TransactionDetails";

const Show = () => {
  return (
    <section>
      <h1>Show</h1>
      <TransactionDetails />
    </section>
  );
};

export default Show;
