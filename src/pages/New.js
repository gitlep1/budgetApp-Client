import NewTransaction from "../components/New/NewTransaction";

const New = () => {
  return (
    <section className="newTransactionSection">
      <NewTransaction />
    </section>
  );
};

export default New;
